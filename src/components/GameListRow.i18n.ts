import type { Dict } from "../i18n/i18n";

export const dict = {
  seeDetails: { "pt-BR": "Ver detalhes de {{title}}", en: "View details for {{title}}" },
  playGame: { "pt-BR": "Jogar {{title}}", en: "Play {{title}}" },
} satisfies Dict;
